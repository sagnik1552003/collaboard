node_modules
# Keep environment variables out of version control
.env

/src/generated/prisma

# build output (backend and frontend both emit to a dist/ folder)
dist
*.tsbuildinfo

# stray compiled artifact of the manual socket test script (keep src/test-socket.ts, drop its build output)
src/test-socket.js

# session notes generated by Claude, not project documentation
CHANGES.md
